import React from 'react';

import { Text, View } from 'react-native';

const Rejected = (props) => {

    return (
        <View><Text>Onboard status</Text></View>
    );
};

export default Rejected;
